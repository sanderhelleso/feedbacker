import React, { Component } from "react";

// Header component
class Header extends Component {
    render() {
        return(
            <div>
                Header
            </div>
        );
    }
}

export default Header;